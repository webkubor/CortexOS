# Vue 3 + Vite + TypeScript + SCSS 模板

> 统一基线：`pnpm + Vue 3 + Vite + TypeScript + SCSS + Ky`

## 使用方式

```bash
pnpm install
pnpm dev
pnpm type-check
pnpm lint
pnpm build
```

## 模板特点

- `pnpm` 为唯一包管理器
- `Ky` 作为统一网络层入口
- `SCSS tokens` 通过 Vite 全局注入
- 自带 `.agent/` AI 协作目录骨架
- 默认启用 Vue Router + Pinia
