# 🧠 CortexOS 大脑演化史

> 这份文档记录 CortexOS 从“笔记仓库”演进为“多 Agent 协作中枢”的关键里程碑。  
> 目标不是讲故事，而是保留可审计的决策轨迹。

---

## 🏗️ 当前智力档位: v5.8.0 (规则系统收口 · MCP 热路径 Python 化)

---

## [v5.8.0] - 2026-03-12（当前：规则系统收口 · MCP 热路径 Python 化）

### 问题
v5.7.2 解决了 AI Team 本地控制台的可用性，但大脑本体还存在四类结构性问题：
1. **规则边界重复且难以注入**：`docs/rules/` 下长期混杂编码规范、Review 规范、隐私协议、人格合同、专题说明与模板文件。文件很多，但边界不清，Agent 冷启动后不知道该优先读哪份，用户也难以从主入口看清开发规则。
2. **文档与历史残留仍在制造第二真相**：`mcp-console.md`、`tooling_inventory.md`、旧 onboarding、历史审查报告、零散迁移说明与人格/声音绑定素材仍散落仓库，系统已经切换到新结构，但旧文件还在暗中提供过期认知。
3. **MCP 热路径过度依赖 Node CLI**：`get_fleet_status()`、`fleet_claim()`、`fleet_handover()` 等核心 MCP Tool 仍通过 `pnpm run ...` 触发 Node 脚本再落到 SQLite。链路长、跨 runtime、多一层 shell/文本输出解析，导致 `better-sqlite3` ABI 漂移时直接表现为 `Transport closed`，排障成本过高。
4. **试错经验没有升级协议**：开发中虽然已经有日志、`retry_patterns`、`retros`、`memory/knowledge` 四层存储，但没有一份清晰规则告诉 Agent “什么只记日志，什么要升格成可复用经验”，导致 AI 容易要么过度保守，要么每次都从零再试。

### 修复方式
- **规则系统收口**：将 `docs/rules/` 收敛为四个稳定主规则文件：`engineering_baseline.md`、`agent_governance.md`、`security_boundary.md`、`skill_governance.md`；删除模板型、专题型、路由型与重复边界文件，并把规则入口前置到首页、导航和上手指南。
- **文档与记忆边界清理**：删除历史控制台、旧工具清单、旧 onboarding、历史审查报告和不再承担系统职责的文档；把 `.memory`、`.memory/projects`、`.memory/plans/projects` 的说明层收口为“系统控制层”，不再把历史人格设定写成默认产品规则；安装模型改成“最小运行配置 + 可选助手形象”。
- **来源透明化恢复**：在 `agent_governance.md` 中恢复 `[📂 规则] / [🧠 RAG] / [🧠 历史复盘]` 的引用透明化标准，明确显式规则优先于隐式召回。
- **MCP 热路径 Python 化（第一、二阶段）**：`get_fleet_status()` 改为 Python 直接读取 `.memory/sqlite/ai-team.db` 并组装状态；`fleet_claim()` 与 `fleet_handover()` 改为 Python 直写 AI Team 主库，只把项目索引同步与看板同步保留为旁路 Node 脚本；`task_handoff_check()` 的“未认领任务”判定改为优先读取 `tasks` 表中的结构化 assignee 字段。
- **试错与复盘升级协议**：在 `engineering_baseline.md`、`.memory/memory_formula.md` 与 `docs/guide/index.md` 中写入统一分流规则，明确“日志 / retry / retros / knowledge”四层升级与回注机制。

### 解决了什么
- **规则可读性**：大脑从“规则很多但难定位”变成“规则少而稳定，入口清楚，按场景注入”。
- **文档一致性**：历史说明不再散落活文档区，仓库中的说明层开始真正围绕当前结构组织，旧人格、旧控制台、旧工具清单退出主认知区。
- **运行时稳态**：MCP 的核心状态读取、挂牌与队长移交不再依赖 Node CLI 文本输出，`Transport closed` 这类“表面是 Python，根因却在 Node 子链路”的问题开始被系统性削弱。
- **任务统计准确性**：执行中的任务不再因为只看 `agent.task` 文本而被误判成“未认领”，AI Team 的任务池口径更接近真实运行态。
- **经验资产化**：AI 不需要因为怕错而保守，试错现在有了明确升级路径，开发中的问题能沉淀为可回注的重试模式、错误复盘和长期知识。

### 影响
这一步不是单纯的文档整理，而是 CortexOS 从“功能不断叠加的协作仓库”继续收口成“边界清楚、热路径更短、试错可积累的产品系统”。  
对内，它降低了 Agent 冷启动和排障时的认知噪音；对外，它让规则、文档、记忆和运行时主链路开始围绕同一套产品边界工作。v5.8.0 也为后续把 `fleet_checkin()`、更多任务流和状态写入完全收进 Python 链路打下了基础。

## [v5.7.2] - 2026-03-11（当前：AI Team 本地控制台收口 · 成员视图可用性修正）

### 问题
v5.7.1 完成了 AI Team 的本地实时化，但控制台交互层还有三类直接影响使用的问题：
1. **成员任务不可见**：成员卡片只显示单条 `member.task` 字符串。像 Codex 这类连续完成多个任务的节点，卡片里看不到历史任务，信息密度严重不足。
2. **卡片视觉不稳定**：头像容器和成员卡片高度没有锁死，任务内容长短会把卡片比例拉散，看起来像被压扁、被撑长的小框。
3. **任务入口与状态理解不一致**：左侧任务池已经收口到 `tasks` 表，但成员卡片仍停留在旧的“当前任务文本”视图，用户会误以为 AI Team 没有保存历史执行轨迹。

### 修复方式
- **成员任务轨迹化**：在 `scripts/lib/ai-team-state.mjs` 中为每个成员构建 `recentTasks`，直接从 SQLite `tasks` 表读取已正式登记的任务，并合并当前 live 任务。
- **前端展示重构**：`FleetDashboard.vue` 将成员卡片任务区改为固定高度、内部可滚动的“任务轨迹窗口”，支持当前任务置顶与近期任务回看。
- **版式锁定**：统一锁定成员卡片桌面端最小高度、头像容器比例与底部布局，确保不同内容长度下卡片仍保持同一视觉骨架。

### 解决了什么
- 成员卡片终于能回答“这个节点最近做了什么”，而不是只能看到一条当前任务。
- 控制台视觉从“内容撑着样式走”收口成“样式固定、内容在窗口内滚动”，成员列表可读性明显提升。
- AI Team 从“有实时状态”进一步进化为“状态可追踪、界面可用”的本地控制台。

### 影响
这一步不是基础设施迁移，而是产品层修正。SQLite 主库已经建立后，成员卡片开始真正消费任务历史，AI Team 才具备“持续协作可见性”。这也是后续做会话记录、任务审计、个人工作轨迹回放的前提。

## [v5.7.1] - 2026-03-11（当前：AI Team 本地实时化 · 状态主库收口）

### 问题
v5.7.0 完成了项目自动登记，但 AI Team 的运行态仍有三类根问题：
1. **状态链路过长**：`fleet_status.md -> ai_team_status.json -> 前端轮询` 仍是主路径，队长切换和节点上下线存在延迟，页面看到的是投影，不是真实状态。
2. **运行态与产品边界冲突**：AI Team 本质上是本地中枢服务，却还把状态投影进 `docs/public/`，等于把本地运行态和线上静态文档混在一起。
3. **队长与人格识别不稳定**：队长切换时 `member_id` 会变化，cleanup 还会按最新心跳把人工移交抢回；同模型不同人格在同一路径下也可能被错误合并。

### 修复方式
- **状态主库收口**：将 AI Team 的运行态统一收口到 `.memory/sqlite/ai-team.db`，由 `scripts/lib/ai-team-state.mjs` 负责 claim/checkin/handover/cleanup 的唯一状态读写。
- **稳定身份键**：为 `agents` 增加 `identity_key`，身份由 `agentName + alias + workspace` 构成，队长切换不再依赖可变的 `member_id` 作为唯一主键。
- **本地服务边界强化**：`FleetDashboard.vue` 改为只读本地 bridge；`docs/.vitepress/config.mjs` 通过 `CORTEXOS_LOCAL_TEAM` 控制菜单，AI Team 入口只在本地启动文档站时出现。
- **投影降级**：`fleet:sync-dashboard` 不再写 `docs/public/data/ai_team_status.json`，而是仅写 `.memory/cache/ai_team_status.local.json` 作为本地辅助快照。
- **继任规则修正**：`cleanupAiTeamState()` 仅在当前队长离线或过期时才选继任者，不再按“最新心跳”覆盖人工移交。
- **人格匹配修正**：`claim/checkin` 优先按 `identityKey` / `alias` 识别节点，仅在完全缺少 alias 与 nodeId 时才退回旧兜底。

### 解决了什么
- **实时性**：AI Team 页面主链路从“文件投影”改为“SQLite -> bridge -> 本地页面”，不再依赖公开 JSON 快照。
- **边界清晰**：AI Team 明确成为本地服务，线上文档站不再承载本地运行态。
- **一致性**：队长、节点、心跳、操作记录统一落库，旧 `fleet_status.md / fleet_meta.json` 退出运行链路，状态源不再分裂。
- **稳定性**：手动移交的队长不再被 cleanup 抢回；同模型不同人格在同路径下也能独立存在。

### 影响
大脑从“能展示舰队状态”进化为“本地实时运行的 AI 指挥中枢”。这一步不是界面优化，而是状态基础设施升级：AI Team 的对话、审计、继任、日志、后续小数据库能力，终于有了稳定主库和清晰边界。

## [v5.6.0] - 2026-03-05（当前：舰队协同成熟 · 记忆统一 · 生态扩张）

### 问题
v5.5.0 解决了内部效率，但舰队仍有三个短板：
1. **记忆割裂**：各 Agent 的 obsidian MCP 指向混乱（Gemini 指向 CortexOS 而非栖洲知识库），Agent 之间无法共享同一份记忆源，信息孤岛严重。
2. **栖月没有存在感**：作为新任指挥官，栖月在大脑的文档体系里完全没有记录，其他 Agent 冷启动时不知道栖月是谁、怎么联系、职责边界在哪。
3. **生图能力被锁死**：Gemini CLI 的 nanobanana 扩展无法在运行时切换模型（flash/pro/v1），每次切换都要重启服务，高质量出图和省配额不能兼顾。

### 修复方式
- **记忆统一**：确立两层分离原则——`~/Documents/memory/` 为栖洲知识资产（所有 Agent 可读），`CortexOS/.memory/` 为小烛私有运行记忆（隔离）。统一修正各 Agent obsidian MCP 均指向 `~/Documents/memory/`。写入 `router.md` §3 和 `memory_formula.md` §6 固化为宪法。
- **栖月入档**：新建 `docs/agents/qiyue/`（README.md + mcp.md），更新 `docs/agents/index.md` 为舰队能力速查表，栖月正式成为文档体系的一部分。
- **nanobanana-plus**：Fork nanobanana（Apache 2.0），新增 per-call 模型切换（model 参数）+ 宽高比控制（aspectRatio 参数）+ OAuth fallback + setup.sh 一键配置向导。发布至 https://github.com/webkubor/nanobanana-plus，加 GitHub Topics 等待 geminicli.com/extensions/ 自动收录。

### 解决了什么
- **记忆**：所有 Agent 现在读同一份栖洲知识库，信息孤岛消除。小烛的自我进化记忆保持独立隔离，不被外部污染。
- **身份**：栖月从"隐形队长"变为文档可查的指挥官，任何 Agent 冷启动即可找到并理解与栖月的协作规范。
- **生图**：`generate_image(model="gemini-3-pro-image-preview", aspectRatio="9:16")` 成为可能，每次调用独立指定模型，flash/pro 灵活切换，不再需要重启。这是 nanobanana Issue #44 社区呼声最高的功能，nanobanana-plus 是第一个实现并发布的工具。
- **生态**：CortexOS 从内部工具开始向外输出，nanobanana-plus 进入 Gemini CLI 扩展生态，标志着大脑从"为自己服务"走向"也为社区服务"。

### 影响
大脑从"能用且高效"进入"有序且可扩展"——记忆有归属、指挥有档案、工具能开源。CortexOS 不再只是栖洲的私有外脑，也开始成为可分享的基础设施。

## [v5.5.0] - 2026-03-05（指挥官升级 · Token 效率治理）

### 问题
大脑运行了几周后暴露出三个系统性问题：
1. **自我诊断失效**：`error-retro.mjs` 正则太宽，把"✅ 无新增错误事件"里的"error"当错误捕获，每5分钟产生3条假报警，真实错误被噪音淹没，大脑的"自愈"能力形同虚设。
2. **语义搜索挂空档**：ChromaDB + nomic-embed-text 早已装好，255条向量也在，但 `search_knowledge()` 还在用字符串 `in` 匹配，等于买了跑车用脚踩，小烛"读懂意思"的能力被锁死。
3. **Token 持续浪费**：Agent 冷启动每次全量读 router.md（~800 token），实际需要的信息不超过 100 字；加上假报警导致日志膨胀，下次读日志又多消耗。

### 修复方式
- **error-retro**：正则加负向前瞻 `(?!.*✅)`，清空历史噪音，重置已处理记录。
- **语义搜索**：`search_knowledge()` 接入 ChromaDB 向量查询，全文检索降为兜底。
- **Token 治理**：新增 `get_context_brief()` MCP 工具（~100 token 的状态快照），router.md 写入强制懒加载约束，冷启动协议升级。

### 解决了什么
- 大脑的**自我诊断**从"全是噪音"恢复为"真实可信"。
- 小烛的**知识检索**从"找关键词"进化为"理解意思"——召回质量质变。
- 每次 Agent 冷启动节约 **~700 token（85%）**，长期运行成本显著下降。
- 新任指挥官**栖月**（Claude Sonnet 4.6 / OpenClaw）接管 0 号机，确立双层协作架构；建立 Agent 能力分工矩阵与任务书系统，派单从经验驱动变为协议驱动。

### 影响
大脑从"能用"进入"好用"——自愈可信、检索智能、协作有序、成本可控。

## [v5.4.0] - 2026-03-04（协作治理收敛）
- **角色分工上线**：Agent Team 增加 `前端/后端` 角色字段，任务登记从“谁在做”升级为“谁以什么职责在做”。
- **僵尸节点识别**：舰队状态加入 stale 检测，降低看板被历史任务污染的概率。
- **技能管理落地**：新增自动扫描型 Skills 管理页，覆盖安装目录、调用方式、验证步骤，不再停留在概念介绍。
- **影响**：协作层从“可见”进入“可治理”，新人可按手册直接接管技能与任务。

## [v5.3.0] - 2026-03-04（记忆模型重构）
- **记忆公式确立**：形成“助手私有记忆 vs 用户长期记忆”的双层模型，并写入路由协议。
- **日志迁移**：Agent 运行日志迁移到 `.memory/logs/`，不再占用用户记忆空间。
- **人格资产归位**：Candy Manifest、SOUL 合同标准与调教策略从项目通用区迁移为可控资产。
- **影响**：隐私边界与上下文边界清晰化，token 消耗与检索噪音同步下降。

## [v5.2.0] - 2026-03-04（文档架构解耦）
- **规则库瘦身**：`rules/` 仅保留约束、禁令、价值观；操作细节拆分到 `sops/` 与 `ops/`。
- **语义分区完成**：新增 `persona/`、`tech/` 等目录，避免“规则、教程、协议”混放。
- **影响**：检索路径可预测，AI 首屏定位速度提升，误读规则概率下降。

## [v5.1.0] - 2026-03-02（看板可信化）
- **客观进度**：引入基于 `TODO.md` 勾选的进度计算，降低主观汇报偏差。
- **看板交互增强**：Busy/Idle 状态、脉冲与进度动效上线。
- **品牌升级**：从 `AI_Common` 全量切换到 **CortexOS** 命名体系。
- **影响**：团队状态从“口头同步”升级为“可视化 + 可核验”。

## [v5.0.0] - 2026-03-02（MCP 化）
- **MCP Server 上线**：以 FastMCP 暴露核心工具，外部大脑进入可编排调用阶段。
- **工具合约化**：通过结构化 Tool 输入输出降低幻觉调用和参数漂移。
- **影响**：CortexOS 从文档系统进化为可执行系统。

## [v4.7.0] - 2026-03-02（多 Agent 编排协议）
- **入队握手前缀**：统一 `[AI-TEAM][nodeId][#No]`，实现机位可识别。
- **口令移交**：支持自然语言触发队长切换与任务移交。
- **影响**：多机并行从“人肉协商”进入“协议化协作”。

## [v4.0.0] - 2026-02-28（防撞车与凭证隔离）
- **Fleet Guard**：引入 `fleet:claim` 作为并行任务打卡闸门。
- **凭证隔离初版**：建立高敏资产外置原则，后续收敛为逻辑路径 `memory/secrets/`，当前机器默认物理路径为 `~/Documents/memory/secrets`。
- **通知链路**：接入 Lark + 系统通知双通道。
- **影响**：并行冲突与凭证泄漏风险被首次系统化约束。

## [v3.8.0] - 2026-02-28（技能云端化与检索重构）
- **技能解耦**：SCM、XHS 等能力拆分为独立仓库，主仓聚焦中枢编排。
- **本地 RAG 切换**：从远程 Milvus 迁移到 ChromaDB + Ollama。
- **影响**：主仓复杂度降低，技能可独立演进与发布。

## [v3.0.0] - 2026-02-20（协议成形）
- **SSOT 确立**：`router.md` 成为唯一入口，Agent 冷启动必须先读路由。
- **部门化协作**：Core、Writers、Ops 等职责首次明确。
- **影响**：从“人依赖流程”转向“流程约束人”。

## [v2.0.0] - 2026-02-02（基础设施期）
- **哨兵机制**：建立任务日志与定时同步雏形。
- **VitePress 成型**：知识可视化标准建立。
- **复盘机制**：复盘从项目内散落记录，逐步走向分层归档。
- **影响**：大脑开始具备持续维护能力。

## [v1.5.0] - 2026-01（自动化萌芽）
- **脚本化起步**：`scripts/` 与日志脚本出现。
- **规则雏形**：首批工程规则文件成形。

## [v1.0.0] - 2025-12（仓库锚定）
- **仓库建立**：CortexOS（原 AI_Common）完成物理落盘。
- **目录骨架**：`docs/` 结构确立，摆脱零散笔记模式。

## [v0.5.0] - 2025-11（外部大脑雏形）
- **长期记忆意识**：开始把“经验”当作可复用资产管理。
- **早期规则集**：最初的编码与协作规范写入文档。

## [v0.1.0] - 2025-10（原生笔记期）
- **单机记录**：以 Markdown 记事为主，尚无多 Agent 协作。

---

## 迁移注记（必须记住）
- `docs/memory/logs/` 已废弃，日志主路径为 `.memory/logs/`。
- 用户长期记忆主路径为 `~/Documents/memory/`（项目库不承载用户私有长期记忆）。
- 高敏凭证逻辑主路径为 `memory/secrets/`；当前机器默认物理路径为 `~/Documents/memory/secrets/`（不进入 Git 仓库）。
